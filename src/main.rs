pub mod models;
pub mod services;
#[cfg(test)]
pub mod tests;

fn main() {
    // receive the input via args().collect()
    // ensure the input is not empty
    // parse the input
    // ensure the result is Ok
    // print the report
}
