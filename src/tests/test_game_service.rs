#[test]
#[ignore = "Not implemented"]
fn test_game_creation() {
    // test the creation of a new game
    // we should have a game with: 
    // total_kills = 0
    // empty players, 
    // empty kills 
    // empty kills_by_means
}

#[test]
#[ignore = "Not implemented"]
fn test_add_kill() {
    // test the addition of a kill to the game
    // we should have a game with:
    // total_kills = 1, 
    // players = ["Player2", "Player1"], 
    // kills = {"Player1": 1} 
    // kills_by_means = {"MOD_RAILGUN": 1}
}
